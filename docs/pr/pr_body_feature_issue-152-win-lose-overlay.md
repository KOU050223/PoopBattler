## 概要

Closes #152

勝ち負けの直後に演出をバトル画面全面へ出し、タップするまで次へ進まないようにします。勝利の次は排便記録、敗北の次は既存の終了画面です。

## 変更内容

- `BattleOutcomeOverlay` を追加（勝利 / 敗北、タップで閉じる）
- バトル画面は勝敗直後も場を残し、演出の下に操作が抜けないよう `inert` にする
- 開発時だけ「即敗北」ボタンを追加（勝利確認用の「即完了」と対）

## 動作確認（AI検証済み）

- [x] overlay テスト（勝利は紙吹雪あり、敗北はなし）
- [x] `npm run typecheck`
- [x] Next MCP `get_compilation_issues` 空
- [x] ブラウザ: 即完了 → 勝利演出 → タップまで排便フォームが出ない → タップ後に Battle Clear
- [x] ブラウザ: 即敗北 → 敗北演出 → タップまで終了ボタンが出ない → タップ後に「バトルを終える」
- [x] Next MCP `get_errors` 空

## ⚠️ 目視確認が必要（人間がマージ前に実施）

> これらは AI が検証できません。**チェックを入れるのは動かして確認した人**です。

### 単体シーンで目視確認

- [ ] 実戦またはデバッグ「即完了」で勝利演出がバトル画面全面に出る
- [ ] タップしないと排便記録へ進まない
- [ ] 全滅またはデバッグ「即敗北」で敗北演出が全面に出る
- [ ] タップしないと「バトルを終える」へ進まない
- [ ] `prefers-reduced-motion` でも文字は出る
- [ ] コンソールにエラーが出ていない

#### 特定シーンからの始め方

1. `/battle` を開いてバトルを始める
2. 開発ビルドなら「即完了」「即敗北」で演出を確認できる
3. 本番相当なら敵撃破または全滅まで進める

#### トラブルシューティング

- **すぐ排便画面になる** → 演出をタップせず待っても進まないか確認。自動進行は入れていない
- **下のボタンが押せる** → 演出中は場の操作を `inert` にしている。再現したらブラウザと画面幅を残す

## 関連

- issue #152: https://github.com/KOU050223/PoopBattler/issues/152

🤖 Generated with [Claude Code](https://claude.com/claude-code) / dev-flow skill
