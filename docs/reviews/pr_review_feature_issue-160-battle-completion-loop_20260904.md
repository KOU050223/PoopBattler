# PR レビューレポート

**PR/ブランチ**: `feature/issue-160-battle-completion-loop`  
**レビュー日時**: 2026-09-04  
**変更規模**: +220 / -17 / 14ファイル（実装）

---

## 変更の概要

Issue #160 の「戦闘中に排便入力へ進む」「勝利画面と入力が行き来する」を、勝敗演出の誤タップと完了後の `/battle` refresh が原因だと見て直した。終了判定（敵撃破・タイムアップ）そのものは変えていない。

**変更種別**:
- [ ] 新機能 (Feature)
- [x] バグ修正
- [ ] リファクタリング
- [ ] パフォーマンス改善
- [x] テスト追加
- [ ] ドキュメント

---

## マージ判定

> **APPROVE**

戦闘中は排便入力に進まないこと、演出を閉じるまで入力に進まないこと、閉じたあとは再マウントしても入力へ戻ることがテストで分かれている。完了 RPC の `/battle` refresh を外しているのも、クライアント状態の巻き戻しを止めるために妥当。ブラウザでの一連操作はこのセッションでは未確認。

---

## 変更ファイル一覧

| ファイル | 変更種別 | 懸念度 |
|---------|---------|--------|
| `src/features/battle/battle-screen-view.ts` | Added | 低 |
| `src/features/battle/battle-screen-view.test.ts` | Added | 低 |
| `src/features/battle/components/battle-outcome-overlay.tsx` | Modified | 低 |
| `src/features/battle/components/battle-screen.tsx` | Modified | 低 |
| `src/stores/battle-store.ts` | Modified | 低 |
| `src/features/battle/actions.ts` | Modified | 低 |
| `src/features/battle/battle-runtime.ts` | Modified | 低 |
| その他テスト・スナップショット | Modified | 低 |

---

## 詳細レビュー

### 画面切り替え

`active` のまま `completion` に進まないケースと、`completing` かつ未確認では演出だけ、確認後は排便入力、を同じヘルパーで見ている。

指摘なし。

### 勝敗演出のロック

全面ボタンが出た直後のクリックを 500ms 無視する。戦闘操作のクリックが演出に乗って入力へ飛ぶ経路を閉じている。演出自体はタップしないと進まない。

指摘なし。

### 永続化

`outcomeAcknowledged` をスナップショットに載せ、ストア version を 2 に上げている。既存の migrate は旧スナップショットを捨てるので、進行中バトルは一度消える。フィールド追加としては既存方針どおり。

### `completeBattleAction`

`revalidatePath("/logs")` と `/collection` は残している。`/battle` だけ外している。バトルページはサーバーから戦闘状態を取っていない。

指摘なし。

---

## 影響範囲

バトル画面の勝敗直後と完了後の遷移だけ。タイムアップ・撃破のルール、完了 RPC の入力契約は変えていない。破壊的変更はクライアントのバトル sessionStorage が version 2 で初期化されること。

---

## テスト確認

- 画面 view: 戦闘中は入力に進まない / 勝利直後は演出 / 確認後は入力: 通過
- 演出ロック: 出してすぐは閉じない / ロック後は閉じられる: 通過
- タイムアップ直前は active、開幕 tick は敵が残っていれば active: 通過
- overlay マークアップに `aria-disabled="true"`: 通過
- `npx tsc --noEmit`: 通過
- 対象 vitest 47 件: 通過
- ブラウザ（agent-browser / `/_next/mcp`）: このセッションでは dev サーバー未起動のため未確認

---

## レビューコメント（コピペ用）

```text
レビューしました。戦闘中に排便入力へ進まないことと、演出確認後に入力へ一度だけ進むことがテストで分かれています。完了時に /battle を refresh しない変更もループ止めとして妥当です。実機の即完了→演出→入力→完了は目視をお願いします。APPROVE です。
```

---

*Generated by Claude Code / pr-review skill*
