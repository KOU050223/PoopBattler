# PR レビューレポート

**PR/ブランチ**: `feature/issue-111-contain-poopm-figure`  
**レビュー日時**: 2026-09-04  
**変更規模**: +125 / -108 / 2ファイル

---

## 変更の概要

`PoopmFigure` の合成パーツが呼び出し側の `h-* w-*` 枠から漏れないように、外枠を `overflow-hidden` に変更し、内側ステージを縮小・下寄せした。既存の重ね順、前後向き、モーション指定は維持している。

**変更種別**:
- [ ] 新機能 (Feature)
- [x] バグ修正 (Bug Fix)
- [ ] リファクタリング
- [ ] パフォーマンス改善
- [x] テスト追加
- [ ] ドキュメント

---

## マージ判定

> **APPROVE**

はみ出しの原因だった `overflow-visible` と、左右・下方向へはみ出す手足の配置を、コンポーネント内部で収まる構造に変更できている。呼び出し側のサイズ指定や画面レイアウトには手を入れておらず、影響範囲は `PoopmFigure` の表示だけに限定されている。

所持キャラが表示される実データ状態での目視は未確認。マージ前に `/collection`、バトル中、バトル完了の表示をスマホ幅とデスクトップ幅で確認する。

---

## 変更ファイル一覧

| ファイル | 変更種別 | 懸念度 |
|---------|---------|--------|
| `src/features/poopm/components/poopm-figure.tsx` | Modified | 問題なし |
| `src/features/poopm/components/poopm-figure.test.tsx` | Modified | 問題なし |

---

## 詳細レビュー

### `src/features/poopm/components/poopm-figure.tsx`

外枠をクリップし、既存の絶対配置パーツを内側ステージにまとめて `translate-y-[12%] scale-[0.7]` で収める変更。パーツごとの座標を大きく作り替えていないため、既存の見た目とモーションの関係を保ちやすい。

指摘事項はなし。

### `src/features/poopm/components/poopm-figure.test.tsx`

小さい枠かつ `eat` モーションでも、外枠クリップと内側ステージが存在することを確認するテストを追加している。既存の前向き/後ろ向き/重ね順テストも維持されている。

指摘事項はなし。

---

## 影響範囲

- `/collection` の先発枠・所持リスト内のキャラクター表示
- `/battle` の敵/味方キャラクター表示
- バトル完了時の仲間獲得カード

**破壊的変更 (Breaking Change)**: なし

---

## テスト確認

| テスト項目 | 状態 |
|-----------|------|
| 変更ファイル lint | パス |
| pre-commit lint | パス |
| `PoopmFigure` 単体テスト | 5 passed |
| Next MCP `get_compilation_issues` | issues なし |
| Next MCP `get_errors` | config/session errors なし |
| ブラウザ表示 | `/collection` と `/battle` は 200。検証セッションは所持0/開始前のためキャラ表示の目視は未確認 |

---

## レビューコメント（コピペ用）

**全体コメント**:
```
レビューしました。APPROVE です。

PoopmFigure 側で外枠クリップと内側ステージ縮小を持つようになり、呼び出し側のカードやバトル画面に個別調整を散らさずに、手足やモーションのはみ出しを抑えられています。
所持キャラがある状態で /collection、バトル中、バトル完了カードを主要幅で目視確認してください。
```

---

## チェックリスト

- [x] Critical issueがすべて解決されている
- [x] テストが追加・更新されている
- [x] 破壊的変更がある場合、マイグレーション手順が記載されている（該当なし）
- [x] セルフレビュー済み

---
*Generated by Cursor agent / pr-review skill*
