# PR レビューレポート

**PR/ブランチ**: `feature/issue-102-timeout-defeat`  
**レビュー日時**: 2026-09-04  
**変更規模**: +42 / -18 / 4ファイル

---

## 変更の概要

タイムアップ時の `resolveByRemainingHp` がパーティ全体のHP割合で `"defeated"` にしていた問題を直した。1体戦闘不能だと割合が大きく下がり、残りが半分程度でも敗北になっていた。敗北は3体全滅だけにし、生存者がいれば `"completing"` へ進む。

**変更種別**:
- [x] バグ修正 (Bug Fix)
- [ ] 新機能
- [ ] リファクタリング
- [ ] ドキュメント

---

## マージ判定

> **APPROVE**

Issue #102 の本丸（タイムアップ=残HP比較の敗北）を、勝敗ステータスを増やさずに潰している。全滅判定は `applyKnockout` のまま。テストは旧実装が落ちるケースと、全滅だけ敗北するケースを同じ `it` に置いている。

引き分け用の新ステータスは入れておらず、タイムアップ生存は既存の完了フロー（ガチャ）に乗る。これは issue 本文の「タイムアップ=敗北にしない」案で、`BattleStatus` と画面を増やさない選択として妥当。

---

## 変更ファイル一覧

| ファイル | 変更種別 | 懸念度 |
|---------|---------|--------|
| `src/features/battle/battle-runtime.ts` | Modified | 低 |
| `src/features/battle/battle-runtime.test.ts` | Modified | 低 |
| `src/features/battle/battle.constants.ts` | Modified | 低 |
| `docs/battle.md` | Modified | 低 |

---

## 詳細レビュー

### `src/features/battle/battle-runtime.ts`

`resolveByRemainingHp` を `resolveTimeout` に改名し、`party.some(hp > 0)` だけを見る。HP割合比較は残していない。無効スナップショット（party/enemy 欠落）は従来どおり `"defeated"`。

指摘事項: なし。

### `src/features/battle/battle-runtime.test.ts`

報告されていた「1体KO + 2体目半分」と、旧テストの「残り1体・HP 10」が完了になること、全滅だけ敗北することを同じ実行で見ている。

指摘事項: なし。

---

## 影響範囲

- タイムアップ生存 → 既存の `completing` → 完了フロー（排便・食事・ガチャ）
- 3体全滅 → 既存の `defeated` 画面
- `BattleStatus` の追加なし。永続化形式の破壊的変更なし

---

## テスト確認

- `npm run lint`: 通過
- `npm test`: 37 files / 191 tests passed
- `npm run typecheck`: 通過

90秒の実バトルタイムアップは自動では踏んでいない。ロジックは `elapsedTicks: TIMEOUT_TICKS` で担保している。

---

## レビューコメント（コピペ用）

```text
レビューしました。タイムアップの敗北を残HP比較から外し、全滅だけに寄せています。新ステータスは増やしておらず、生存タイムアップは既存の完了フローに乗ります。テストも通っているため APPROVE です。
```

---

*Generated by Cursor agent / pr-review skill*
