# PR レビューレポート

**PR/ブランチ**: `feature/issue-108-inventory-party`  
**レビュー日時**: 2026-09-04  
**変更規模**: +871 / -64 / 17ファイル

---

## 変更の概要

`/collection` を図鑑からインベントリへ置き換え、先発3枠と所持リストの入れ替えでパーティを組めるようにする。バトル開始はクライアントから `user_characters.id` だけを受け取り、HP / Power / Speed はサーバーが `user_characters` から読む。他人のIDは RPC が落とし、不足枠は既存どおりレンタルで埋める。

**変更種別**:
- [x] 新機能 (Feature)
- [ ] バグ修正 (Bug Fix)
- [ ] リファクタリング
- [ ] パフォーマンス改善
- [x] テスト追加
- [ ] ドキュメント

---

## マージ判定

> **APPROVE**

完了条件（見出し・ナビ、先発3枠、選択IDでの開始、不足時レンタル、3値をクライアントから渡さない、開始テストの更新）を満たしている。セキュリティ境界は既存の `start_battle` RPC に寄せてあり、TS 側は ID の整形とスナップショットの採用に留まっている。lint / 全テスト / tsc / Next MCP compile は通過した。

入れ替え操作のブラウザ確認は、この検証セッションの匿名ユーザーが所持0のため未実施。マージ前の目視項目に残す。

---

## 変更ファイル一覧

| ファイル | 変更種別 | 懸念度 |
|---------|---------|--------|
| `src/features/collection/party-lineup.ts` | Added | 🟢 問題なし |
| `src/features/collection/party-lineup.test.ts` | Added | 🟢 問題なし |
| `src/features/collection/components/inventory-screen.tsx` | Added | 🟢 問題なし |
| `src/features/collection/components/inventory-screen.test.tsx` | Added | 🟢 問題なし |
| `src/features/collection/components/collection-list.tsx` | Modified | 🟢 問題なし |
| `src/features/collection/actions.ts` | Modified | 🟢 問題なし |
| `src/features/collection/character.types.ts` | Modified | 🟢 問題なし |
| `src/app/(app)/collection/page.tsx` | Modified | 🟢 問題なし |
| `src/components/layout/navigation.ts` | Modified | 🟢 問題なし |
| `src/features/battle/start-battle.ts` | Modified | 🟢 問題なし |
| `src/features/battle/actions.ts` | Modified | 🟢 問題なし |
| `src/features/battle/components/battle-screen.tsx` | Modified | 🟢 問題なし |
| `src/features/battle/start-battle.test.ts` | Modified | 🟢 問題なし |
| `src/features/battle/rental-party.ts` | Modified | 🟢 問題なし |
| その他テスト・文言 | Modified | 🟢 問題なし |

---

## 詳細レビュー

### `src/features/battle/start-battle.ts`

#### 変更の意図

`userCharacterIds` を受け取って RPC へ渡す。空のときは従来どおり新しい順の自動選出に戻す（インベントリ未訪問）。`readStartBattleUserCharacterIds` は UUID 以外と 3 値フィールドを捨てる。

#### 指摘事項

なし。他人の ID の除外は RPC の既存仕様に任せ、TS は `party_snapshot` を正としている。選択済みが3体未満・不正ID混入・自動選出を読まないケースがテストされている。

### `src/features/collection/party-lineup.ts`

#### 変更の意図

先発IDの保存・復元・入れ替えを純関数に切り出し、画面とバトル開始で同じ並びを使う。

#### 指摘事項

なし。所持から外れた ID は落とし、足りなければ新しい順で埋める。空きは左詰めしてレンタルを右に寄せる。

### `src/features/collection/components/inventory-screen.tsx`

#### 変更の意図

先発3枠と所持リスト。枠を選んでからリストで入れ替える。レンタル枠は表示のみ。

#### 指摘事項

なし。編成の読み書きは `useSyncExternalStore` で、effect 内 `setState` を避けている。

---

## 影響範囲

- `/collection` の表示名と操作（ルートは互換のため残置）
- `startBattleAction` の引数（任意の `userCharacterIds`。省略時は従来の自動選出）
- ボトムナビ・バトル完了画面の「インベントリ」文言
- スキーマ / RPC / 認証は未変更

**破壊的変更 (Breaking Change)**: なし（開始アクションは引数省略で旧挙動）

---

## テスト確認

| テスト項目 | 状態 |
|-----------|------|
| 既存テストがパスするか | ✅ 242 passed |
| 新機能のテストが追加されているか | ✅ 選出ID / 不足レンタル / 不正ID / 3値無視 / 入れ替え |
| エッジケースのテスト | ✅ 所持0・2体・重複・壊れた JSON |
| ブラウザでの入れ替え | ❓ 所持0のセッションでは未確認 |

---

## レビューコメント（コピペ用）

**全体コメント**:
```
レビューしました。APPROVE です。

クライアントは user_characters.id だけを渡し、3値はサーバー確定のままです。
所持がいるアカウントで、先発枠タップ → リスト入れ替え → その3体で開始、だけ目視してください。
```

---

## チェックリスト

- [x] Critical issueがすべて解決されている
- [x] テストが追加・更新されている
- [x] 破壊的変更がある場合、マイグレーション手順が記載されている（該当なし）
- [x] セルフレビュー済み

---
*Generated by Cursor agent / pr-review skill*
