# PR レビューレポート

**PR/ブランチ**: `feature/issue-120-speed-guard-cooldown` issue #122 追加分  
**レビュー日時**: 2026-09-04  
**対象コミット**: `68eea97 feat: issue #122 add special charge swirl`  
**変更規模**: +85 / -8 / 3ファイル

## 変更の概要

必殺技の踏ん張り中だけ、場に出ている味方うんちくんの周囲にチャージ用のグルグル演出を表示する。敵と控えメンバーには表示せず、既存の攻撃・被弾・アイドルモーションは外側コンテナ側に残している。

## マージ判定

**APPROVE**

差分は表示レイヤーとその呼び出しに閉じており、バトルロジックや永続化へ影響しない。`prefers-reduced-motion` 時は回転を止め、静止した演出に落としているため、受け入れ条件に沿っている。

## 変更ファイル一覧

| ファイル | 変更種別 | 懸念度 |
| --- | --- | --- |
| `src/features/battle/components/battle-figure.tsx` | Modified | 低 |
| `src/features/battle/components/battle-screen.tsx` | Modified | 低 |
| `src/features/battle/components/battle-figure.test.tsx` | Added | 低 |

## 詳細レビュー

### `src/features/battle/components/battle-figure.tsx`

`ChargeSwirl` は `BattleFigure` 内の `PoopmFigure` 背面にだけ追加される。既存の `battleFigureAnimate` は外側の `motion.div` に残っているため、攻撃・被弾・アイドルの位置アニメーションを上書きしていない。

指摘事項: なし。

### `src/features/battle/components/battle-screen.tsx`

`charging={snapshot.playerStance === "special"}` は味方側の現在メンバー描画にだけ渡されている。敵側の `BattleFigure` と控えメンバー表示には渡していない。

指摘事項: なし。

### `src/features/battle/components/battle-figure.test.tsx`

`charging` 未指定ではエフェクトが出ないこと、`charging` 指定時だけ出ることを同じテストで確認している。

指摘事項: なし。

## テスト確認

- `npm run test -- src/features/battle/components/battle-figure.test.tsx src/features/battle/battle-figure.motion.test.ts`: 5 tests passed
- `npm run lint -- src/features/battle/components/battle-figure.tsx src/features/battle/components/battle-screen.tsx src/features/battle/components/battle-figure.test.tsx`: passed
- pre-commit hook の `npm run lint`: passed
- `npm run typecheck`: passed
- Next MCP `get_compilation_issues`: `issues: []`
- Next MCP `get_errors`: config/session errors なし
- `agent-browser` で `/battle` を表示し、検証用の必殺準備中状態で `allyHasChargeArc: true`, `enemyHasChargeArc: false`, `chargeArcCount: 1` を確認

## 残る確認

実機の揺れ入力中に、チャージ演出が過剰に邪魔にならないかは人間の目視で確認する。

---

Generated by Cursor agent / pr-review skill
